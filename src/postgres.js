const postgresTypes = {
  TEXT: 'TEXT',
  BOOL: 'BOOL',
  TIMESTAMP: 'TIMESTAMP', // date + time (UTC)
  JSONB: 'JSONB', // blob of data
  ARRAY: 'ARRAY',
}
module.exports.postgresTypes = postgresTypes
